---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('thinking'))
  .filter((post) => !post.data.draft)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());
---

<BaseLayout>
  <section class="hero">
    <p>
      Ici je partage ce que j'observe au quotidien dans la construction et l'immobilier en Suisse — rénovation énergétique, gestion de chantier, réglementation, valorisation.
    </p>
  </section>

  <hr class="separator" />

  <section class="section reveal">
    <h2>Dernières réflexions</h2>
    <ul class="post-list">
      {posts.map((post) => (
        <li>
          <a href={`/reflexions/${post.id}`}>
            <span class="date">
              {post.data.date.toLocaleDateString('fr-CH', {
                year: 'numeric',
                month: '2-digit',
              })}
            </span>
            <span class="title">{post.data.title}</span>
          </a>
        </li>
      ))}
    </ul>
    <p style="margin-top: 1rem;">
      <a href="/reflexions">Toutes les réflexions →</a>
    </p>
  </section>

  <hr class="separator" />

  <section class="section reveal">
    <h2>Réalisations</h2>
    <p class="muted" style="margin-bottom: 1rem;">Projets personnels et explorations techniques.</p>
    <div>
      <a href="/realisations" style="text-decoration: none; color: inherit;">
        <div class="card">
          <h3>Centre Sportif de Malley</h3>
          <p class="desc">Patinoire & Arena — phases SIA 41 à 52, Prilly.</p>
        </div>
      </a>
      <a href="/realisations" style="text-decoration: none; color: inherit;">
        <div class="card">
          <h3>Étoile-Palettes — 280 logements</h3>
          <p class="desc">Rénovation en site occupé, Grand-Lancy.</p>
        </div>
      </a>
    </div>
    <p style="margin-top: 0.5rem;">
      <a href="/realisations">Toutes les réalisations →</a>
    </p>
  </section>
</BaseLayout>
